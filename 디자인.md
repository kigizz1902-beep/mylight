# 반려 도깨비불 앱 디자인 가이드

> 문서 목적: 이 파일 하나만 읽고도 디자인 경험이 없는 개발자 또는 AI 코딩 도구가 동일한 시각 언어로 화면을 구현할 수 있도록 한다.
>
> 적용 대상: 반려 도깨비불 모바일 웹 앱의 메인 화면, 기록 화면, 성장 화면, 설정 화면 및 공통 컴포넌트
>
> 디자인 스타일명: **Aurora Glassmorphism + Glow Bento UI**  
> 한국어 정의: **어두운 밤의 공간 위에 도깨비불의 상태와 기억이 작은 빛의 카드로 떠오르는 오로라 글로우 벤토 UI**

---

## 0. 이 문서를 사용하는 방법

개발을 시작하기 전에 아래 순서를 지킨다.

1. `1. 디자인의 핵심 인상`과 `2. 절대 규칙`을 먼저 읽는다.
2. `5. 디자인 토큰`의 색상과 수치를 CSS 변수로 등록한다.
3. 공통 배경 → 앱 셸 → 카드 → 버튼 → 개별 화면 순서로 구현한다.
4. 임의의 색상, 그림자, 모서리 값은 만들지 않는다. 이 문서의 토큰을 재사용한다.
5. 마지막에 `17. 완성도 검수 체크리스트`로 결과물을 점검한다.

이 문서에서 **MUST**는 반드시 지켜야 하는 규칙, **SHOULD**는 특별한 이유가 없다면 지키는 규칙이다.

---

## 1. 디자인의 핵심 인상

이 앱은 일반적인 스마트 조명 제어 앱처럼 보이면 안 된다. 사용자가 조명의 수치를 조작하는 느낌보다, **어두운 밤에 작은 존재의 상태를 살피고 함께 시간을 보내는 느낌**이 먼저 와야 한다.

화면을 보았을 때 다음 세 문장이 동시에 느껴져야 한다.

- 방 전체는 조용하고 어둡다.
- 어둠 속 몇 개의 카드에만 빛이 머물러 있다.
- 가장 따뜻한 빛은 기능이 아니라 살아 있는 도깨비불에서 나온다.

### 1.1 감정 키워드

- 고요함
- 야간의 친밀함
- 부드러운 미래감
- 신비로움
- 작은 생명감
- 손으로 만질 수 있을 듯한 말랑함
- 오랫동안 곁에 머문 흔적

### 1.2 피해야 할 인상

- 사이버펑크 게임 HUD
- 화려한 RGB 게이밍 기기
- 일반적인 스마트홈 대시보드
- 밝고 귀여운 육성 게임
- 유리 효과만 과도한 글래스모피즘
- 블루와 퍼플이 난무하는 평범한 AI SaaS 화면
- 모든 정보가 동일한 강도로 빛나는 화면

### 1.3 시각적 우선순위

화면에서 사용자의 시선은 반드시 아래 순서로 이동해야 한다.

1. 살아 있는 도깨비불 또는 현재 상태
2. 오늘 함께한 시간과 핵심 메시지
3. 밝기·성장·기록 등 보조 정보
4. 메뉴, 설정, 기술적 상태

---

## 2. 절대 규칙

### MUST 01 — 배경은 검정이 아니라 ‘깊이가 있는 밤’이어야 한다

단일 `#000000` 배경을 사용하지 않는다. 차콜 블랙을 기본으로 하고, 화면 가장자리나 하단에 아주 희미한 딥 인디고와 블루그레이 환경광을 겹친다.

### MUST 02 — 한 화면의 주인공 글로우는 하나뿐이다

현재 도깨비불 카드 또는 핵심 행동 카드 하나만 가장 밝게 발광한다. 나머지 카드는 낮은 대비의 검은 유리 표면으로 남겨둔다.

### MUST 03 — 따뜻한 색은 생명, 차가운 색은 공간이다

- 크림·앰버·구운 오렌지: 도깨비불, 생명, 교감, 함께한 시간
- 딥 블루·인디고·블루그레이: 밤, 공간, 기록, 비활성 상태
- 라일락: 성장, 특별한 기억, 드문 변화
- 초록·빨강: 시스템 성공·오류에만 제한적으로 사용

### MUST 04 — 빛에는 광원이 있어야 한다

아무 이유 없이 카드 전체에 무지개 그라데이션을 넣지 않는다. 모든 글로우는 불꽃, 상태 점, 그래프의 활성 지점, 버튼의 핵심 등 **명확한 광원**에서 퍼져야 한다.

### MUST 05 — 텍스트는 빛나지 않는다

본문과 제목에 네온 `text-shadow`를 사용하지 않는다. 텍스트는 선명하고 차분하게 유지하고, 빛은 배경과 오브젝트에만 사용한다.

### MUST 06 — 모서리는 둥글지만 장난감처럼 보이지 않아야 한다

큰 카드는 24–28px, 작은 카드와 버튼은 16–22px 범위로 통일한다. 모든 요소를 과도하게 캡슐형으로 만들지 않는다.

### MUST 07 — 정보보다 상태를 먼저 말한다

`밝기 12%`보다 `잠들 준비를 하고 있어요`를 먼저 보여주고, 수치는 보조 정보로 배치한다.

---

## 3. 전체 화면의 시각 구조

화면은 네 겹으로 생각한다.

### Layer 1 — Night Space

가장 아래의 밤 배경. 차콜 블랙과 딥 네이비가 섞인 정적인 공간이다.

### Layer 2 — Ambient Light

화면 가장자리와 하단에 흐릿하게 번지는 환경광. 사용자가 직접 인식하기보다 배경에 깊이를 느끼게 해야 한다.

### Layer 3 — Glass Bento Cards

정보를 담는 둥근 유리 카드. 반투명하지만 뒤가 훤히 보이는 유리가 아니라, 검은 표면 안에 미세한 빛이 갇힌 재질이다.

### Layer 4 — Living Glow

도깨비불, 현재 상태 점, 활성 버튼 등 살아 있는 반응. 가장 높은 대비와 따뜻한 색을 사용한다.

---

## 4. 기본 화면 기준

### 4.1 디자인 기준 크기

- 모바일 기준 아트보드: `390 × 844px`
- 최소 대응 너비: `320px`
- 모바일 최대 콘텐츠 너비: `480px`
- 데스크톱 앱 셸 최대 너비: `520px`
- 데스크톱에서는 앱 셸을 중앙에 두고 주변을 어두운 배경으로 유지한다.

### 4.2 안전 영역과 여백

- 좌우 기본 여백: `20px`
- 360px 이하 좌우 여백: `16px`
- 상단 여백: `max(20px, env(safe-area-inset-top))`
- 하단 내비게이션 아래: `max(16px, env(safe-area-inset-bottom))`
- 카드 사이 간격: `12px`
- 큰 섹션 사이 간격: `24–32px`
- 카드 내부 여백: 기본 `20px`, 작은 카드 `16px`

### 4.3 그리드

- 기본: 2열 벤토 그리드
- 열 간격: `12px`
- 핵심 카드는 2열 전체를 사용한다.
- 보조 카드는 1열씩 사용하되, 높이가 지나치게 제각각 되지 않게 한다.
- 콘텐츠를 억지로 2열에 넣어 글자가 줄줄이 꺾이면 1열 전체로 전환한다.

---

## 5. 디자인 토큰

아래 변수를 전역 CSS에 먼저 등록한다. 페이지 안에서 새로운 색을 직접 입력하지 않는다.

```css
:root {
  /* Background */
  --night-950: #07080b;
  --night-900: #0b0d12;
  --night-850: #10131a;
  --night-800: #151924;

  /* Glass surfaces */
  --glass-strong: rgba(24, 28, 38, 0.78);
  --glass-medium: rgba(20, 23, 32, 0.64);
  --glass-soft: rgba(28, 32, 43, 0.42);
  --glass-border: rgba(255, 255, 255, 0.10);
  --glass-border-soft: rgba(255, 255, 255, 0.06);
  --glass-highlight: rgba(255, 255, 255, 0.14);

  /* Text */
  --text-primary: #f4f1ea;
  --text-secondary: #b7bac4;
  --text-tertiary: #777d8c;
  --text-disabled: #505562;

  /* Living flame */
  --flame-core: #fff4d6;
  --flame-cream: #f9dca0;
  --flame-amber: #e99b45;
  --flame-deep: #a95524;
  --flame-ember: #6e2f1e;

  /* Night ambience */
  --aurora-blue: #315a87;
  --aurora-indigo: #343b78;
  --aurora-violet: #69558a;
  --rain-blue: #486275;

  /* Semantic */
  --success: #74a58f;
  --warning: #d1a15f;
  --error: #bf6969;

  /* Radius */
  --radius-xl: 28px;
  --radius-lg: 24px;
  --radius-md: 18px;
  --radius-sm: 14px;
  --radius-pill: 999px;

  /* Spacing */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;

  /* Shadow */
  --shadow-card: 0 18px 50px rgba(0, 0, 0, 0.34);
  --shadow-inset: inset 0 1px 0 rgba(255, 255, 255, 0.07);
  --shadow-flame: 0 0 18px rgba(233, 155, 69, 0.42),
                  0 0 48px rgba(169, 85, 36, 0.24);

  /* Motion */
  --ease-soft: cubic-bezier(0.22, 1, 0.36, 1);
  --ease-breathe: cubic-bezier(0.45, 0, 0.55, 1);
  --duration-fast: 160ms;
  --duration-normal: 280ms;
  --duration-slow: 900ms;
}
```

### 5.1 색상 사용 비율

한 화면의 대략적인 면적 비율은 다음과 같다.

- 차콜·블랙·딥 네이비: `75–82%`
- 검은 유리 카드: `12–18%`
- 블루·인디고 환경광: `3–6%`
- 앰버·크림 생명광: `1–3%`
- 라일락 또는 의미색: `1% 미만`

색이 많아 보인다면 이미 과한 것이다.

---

## 6. 배경 제작 규칙

### 6.1 기본 배경

```css
body {
  min-height: 100dvh;
  color: var(--text-primary);
  background:
    radial-gradient(circle at 82% 18%, rgba(49, 90, 135, 0.12), transparent 34%),
    radial-gradient(circle at 12% 82%, rgba(52, 59, 120, 0.11), transparent 38%),
    linear-gradient(180deg, #0d1016 0%, var(--night-950) 58%, #08090d 100%);
  background-attachment: fixed;
}
```

### 6.2 환경광

- 블러 반경은 `70–140px`로 크게 사용한다.
- 불투명도는 `0.08–0.18`을 넘지 않는다.
- 화면 구석에 걸쳐 배치해 원형 그라데이션의 전체 모양이 보이지 않게 한다.
- 배경에 빨강, 선명한 핑크, 민트는 사용하지 않는다.
- 노이즈 텍스처를 사용할 경우 불투명도는 `2–3%`만 적용한다.

### 6.3 배경 금지 사례

- 무지개 메시 그라데이션
- 중앙에서 강하게 터지는 파란 빛
- 눈에 띄는 별, 파티클, 우주 배경
- 반복되는 큰 블롭 모양
- 움직임이 분명한 배경 애니메이션

---

## 7. 카드 디자인

카드는 이 디자인의 핵심이다. 카드가 단순한 회색 박스로 보이면 실패이고, 모든 카드가 네온 간판처럼 보이면 더 큰 실패다.

### 7.1 기본 카드

```css
.glass-card {
  position: relative;
  overflow: hidden;
  padding: 20px;
  border: 1px solid var(--glass-border-soft);
  border-radius: var(--radius-lg);
  background:
    linear-gradient(145deg, rgba(255, 255, 255, 0.045), transparent 38%),
    var(--glass-medium);
  box-shadow: var(--shadow-inset), var(--shadow-card);
  backdrop-filter: blur(22px) saturate(115%);
  -webkit-backdrop-filter: blur(22px) saturate(115%);
}
```

### 7.2 카드 표면 규칙

- 상단 또는 좌측 상단에 매우 약한 하이라이트를 둔다.
- 테두리는 1px을 유지한다.
- 카드 바깥 그림자는 검은색으로 깊이만 만든다.
- 컬러 글로우는 `::before` 또는 `::after` 가상 요소로 카드 안쪽에 가둔다.
- 카드 표면의 글로우는 카드 면적의 30%를 넘지 않는다.
- 카드 안에 또 카드가 들어갈 경우 내부 카드는 배경 대비만 사용하고 강한 그림자를 추가하지 않는다.

### 7.3 카드 종류

#### A. Living Card — 현재 도깨비불

- 화면에서 가장 크고 밝은 카드
- 2열 전체 너비
- 최소 높이 `260px`, 권장 `300–340px`
- 불꽃 오브젝트가 카드 면적의 `35–45%`를 차지
- 따뜻한 앰버 글로우 사용
- 상태 문장은 불꽃보다 아래 또는 좌측 하단에 배치
- 숫자와 기술 상태는 가장 아래의 작은 텍스트로 배치

#### B. Memory Card — 함께한 시간과 기록

- 딥 블루·인디고 글로우
- 숫자는 크지만 발광하지 않음
- 날짜나 단위는 작은 회색 텍스트
- 그래프는 얇고 흐릿하게, 활성 지점 하나만 밝게 표현

#### C. Growth Card — 성장 단계

- 인디고에서 라일락으로 이어지는 제한된 그라데이션
- 성장 변화가 있을 때만 라일락이 선명해짐
- 별·보석·레벨 배지 대신 불씨의 크기, 흔적, 결의 변화를 사용

#### D. Control Card — 밝기와 조명 제어

- 장식보다 조작성 우선
- 따뜻한 색은 활성 트랙이나 손잡이에만 사용
- 비활성 트랙은 `rgba(255,255,255,0.09)`
- `전원 켜짐`보다 `곁에 있어요`처럼 세계관 언어를 우선 사용

#### E. System Card — 연결과 설정

- 가장 어둡고 조용한 카드
- 글로우를 사용하지 않거나 아주 약한 블루만 사용
- 시스템 정보가 메인 화면보다 눈에 띄면 안 됨

### 7.4 카드 내부 정보 순서

```text
작은 라벨 → 핵심 상태 또는 큰 값 → 보조 설명 → 선택적 조작 요소
```

예시:

```text
오늘의 불씨
조용히 깨어 있어요
함께한 지 2시간 14분
[밝기 슬라이더]
```

---

## 8. 도깨비불 오브젝트 표현

도깨비불은 일반적인 불꽃 아이콘이나 전구 아이콘이 아니다. **크림색 중심과 앰버색 외곽을 가진 비대칭 유기체**처럼 보여야 한다.

### 8.1 형태

- 완벽한 원, 물방울, 하트 모양을 피한다.
- 좌우가 미세하게 비대칭이어야 한다.
- 중심은 둥글지만 끝부분은 한쪽이 살짝 길거나 찌그러져도 좋다.
- 외곽선 없이 빛과 투명도 차이로 형태를 만든다.
- 이모지 `🔥`, 전구 아이콘, 날카로운 불꽃 SVG는 사용하지 않는다.

### 8.2 빛의 세 층

1. **Core:** 작고 밝은 크림 화이트, 선명도 80%
2. **Body:** 부드러운 앰버, 형태를 결정하는 본체
3. **Halo:** 딥 오렌지 브라운, 넓고 흐린 잔광

### 8.3 권장 CSS 구조

```css
.flame {
  position: relative;
  width: 104px;
  aspect-ratio: 0.86;
  border-radius: 56% 44% 52% 48% / 64% 58% 42% 36%;
  background:
    radial-gradient(circle at 48% 58%, var(--flame-core) 0 13%, transparent 30%),
    radial-gradient(circle at 50% 56%, var(--flame-cream) 0 25%, var(--flame-amber) 52%, var(--flame-deep) 78%);
  filter: blur(0.2px);
  box-shadow: var(--shadow-flame);
  transform-origin: 50% 78%;
}

.flame::after {
  content: "";
  position: absolute;
  inset: -34%;
  z-index: -1;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(233, 155, 69, 0.26), transparent 67%);
  filter: blur(18px);
}
```

실제 프로젝트에 전용 이미지나 애니메이션 에셋이 있다면 그것을 우선 사용하고, 위 CSS는 임시 프로토타입에만 사용한다.

---

## 9. 타이포그래피

### 9.1 폰트

한국어와 영문을 함께 안정적으로 표시하기 위해 아래 순서로 사용한다.

```css
font-family: Pretendard, "SUIT Variable", SUIT, Inter, -apple-system,
  BlinkMacSystemFont, "Segoe UI", sans-serif;
```

### 9.2 크기 체계

| 용도 | 크기 / 행간 | 굵기 | 색상 |
|---|---:|---:|---|
| 화면 제목 | 28px / 1.25 | 650 | `--text-primary` |
| 핵심 상태 | 24px / 1.35 | 600 | `--text-primary` |
| 큰 숫자 | 32–40px / 1.05 | 620 | `--text-primary` |
| 카드 제목 | 17px / 1.4 | 600 | `--text-primary` |
| 본문 | 15px / 1.6 | 400 | `--text-secondary` |
| 작은 라벨 | 12px / 1.4 | 550 | `--text-tertiary` |
| 보조 수치 | 12–13px / 1.4 | 450 | `--text-tertiary` |

### 9.3 타이포 규칙

- 제목에 전부 대문자를 사용하지 않는다.
- 화면당 가장 큰 텍스트는 한 개만 둔다.
- 자간은 기본값 또는 `-0.01em`을 사용한다.
- 작은 영문 라벨에만 `0.04em`까지 허용한다.
- 긴 설명보다 한두 줄의 상태 문장을 우선한다.
- 가운데 정렬은 도깨비불 중심 카드에만 제한적으로 사용한다.
- 그 외 정보 카드는 좌측 정렬한다.

---

## 10. 버튼과 조작 요소

### 10.1 Primary Button

- 높이: `52px`
- 좌우 패딩: `22px`
- 모서리: `18px` 또는 캡슐형
- 배경: 앰버 단색이 아니라 어두운 앰버 그라데이션
- 텍스트: 크림 화이트
- 외곽 글로우는 약하게 사용

```css
.button-primary {
  min-height: 52px;
  padding: 0 22px;
  border: 1px solid rgba(255, 226, 174, 0.18);
  border-radius: var(--radius-md);
  color: #fff5df;
  background: linear-gradient(135deg, #b96d32, #8b4527);
  box-shadow: 0 10px 28px rgba(169, 85, 36, 0.22),
              inset 0 1px 0 rgba(255,255,255,0.16);
}
```

### 10.2 Secondary Button

- 투명한 검은 유리 표면
- `1px` 흰색 8–10% 테두리
- 그림자는 거의 없음
- hover 또는 pressed 상태에서만 표면이 약간 밝아짐

### 10.3 Icon Button

- 최소 터치 영역: `44 × 44px`
- 보이는 원형 또는 둥근 사각형 크기: `40–44px`
- 아이콘 선 두께: `1.7–2px`
- 아이콘 컬러: 기본 `--text-secondary`, 활성 시 `--flame-cream`

### 10.4 상태 태그

태그는 작고 조용한 캡슐이다.

- 높이: `26–30px`
- 패딩: `0 10px`
- 글자 크기: `11–12px`
- 배경: 관련 색상 10–14% 투명도
- 테두리: 관련 색상 16–20% 투명도

권장 문구:

- 곁에 있음
- 졸고 있음
- 따라가는 중
- 잠들 준비
- 오늘은 조금 흐림
- 기억이 자라는 중

### 10.5 슬라이더

- 트랙 높이: `5–6px`
- 손잡이: `20–22px`
- 손잡이 주위에 앰버 헤일로를 약하게 적용
- 현재 값 숫자는 트랙 바로 옆에 작게 표시
- 밝기 변경 중에는 도깨비불 오브젝트도 동시에 반응해야 한다.

---

## 11. 아이콘과 그래픽

- 아이콘 스타일: 둥근 모서리의 단순한 라인 아이콘
- 권장 라이브러리: Lucide Icons
- 한 화면 안에서 아이콘 라이브러리를 섞지 않는다.
- 기본 크기: `20px`, 내비게이션 `22px`, 작은 보조 아이콘 `16px`
- 채워진 아이콘은 현재 선택된 내비게이션이나 중요한 상태에만 사용한다.
- 장식용 아이콘을 반복하지 않는다.
- 아이콘에 컬러 그라데이션을 넣지 않는다.

### 그래프와 기록 시각화

- 축과 격자선은 최소화한다.
- 선 두께는 `1.5–2px`.
- 비활성 데이터는 블루그레이 20–30%.
- 오늘 또는 선택 지점만 크림·앰버로 강조한다.
- 데이터가 없는 상태를 가짜 그래프로 채우지 않는다.
- 복잡한 차트보다 작은 빛의 점, 얇은 궤적, 누적 링을 우선한다.

---

## 12. 하단 내비게이션

권장 탭은 최대 4개다.

- 오늘
- 기억
- 성장
- 설정

### 시각 규칙

- 앱 셸 하단에 고정한다.
- 배경: `rgba(13, 15, 21, 0.82)`
- 상단 테두리: 흰색 6%
- `backdrop-filter: blur(24px)`
- 높이: 안전 영역 제외 `68–72px`
- 활성 탭은 아이콘과 텍스트를 크림 또는 옅은 앰버로 표시한다.
- 활성 탭 배경을 큰 컬러 캡슐로 감싸지 않는다.
- 비활성 탭은 `--text-tertiary`.

---

## 13. 메인 화면 구성 명세

메인 화면은 다음 순서로 구현한다.

### 13.1 Header

- 좌측: 시간에 따른 짧은 인사 또는 날짜
- 우측: 알림 또는 설정 아이콘 버튼 한 개
- 로고를 크게 반복하지 않는다.
- 헤더 높이: 약 `56px`

예시 문구:

```text
비가 오는 밤이에요
도깨비불은 조용히 깨어 있어요.
```

### 13.2 Living Card

메인 화면의 중심이다.

- 카드 상단 작은 상태 태그: `곁에 있음`
- 중앙: 도깨비불 오브젝트
- 하단 큰 상태 문장: `조용히 깨어 있어요`
- 보조 문장: `오늘 2시간 14분을 함께했어요`
- 필요할 경우 밝기 슬라이더를 카드 최하단에 둔다.

카드 내부 요소끼리 겹치지 않으며, 도깨비불 주변에 최소 `32px`의 빈 공간을 둔다.

### 13.3 Bento Row 1

왼쪽 카드 — 함께한 시간

- 작은 라벨: `오늘 함께한 시간`
- 큰 숫자: `2h 14m`
- 어제 대비 정보는 가장 아래 작게 표시

오른쪽 카드 — 성장 상태

- 작은 라벨: `불씨의 성장`
- 상태: `두 번째 온기`
- 작은 유기적 점 또는 링 그래픽

### 13.4 Memory Card

- 2열 전체 사용
- 제목: `이번 주의 밤`
- 최근 7일의 함께한 시간을 얇은 선 또는 빛 점으로 표시
- 가장 오래 함께한 날만 앰버로 강조
- 카드 클릭 시 기록 화면으로 이동

### 13.5 Quiet Action

메인 화면 하단에는 큰 CTA를 반복하지 않는다. 필요한 경우 한 개만 둔다.

예시:

- 오늘의 기억 보기
- 잠들 준비하기
- 도깨비불 부르기

---

## 14. 상태별 화면 변화

도깨비불의 상태는 텍스트만 바꾸지 말고 빛의 강도와 움직임을 함께 바꾼다.

| 상태 | 불꽃 밝기 | 움직임 | 카드 색 | 문구 예시 |
|---|---:|---|---|---|
| 연결 전 | 15% | 없음 | 블루그레이 | 아직 서로를 찾는 중이에요 |
| 깨어 있음 | 70% | 느린 호흡 | 앰버 | 조용히 깨어 있어요 |
| 함께 이동 | 78% | 짧은 좌우 흔들림 | 앰버+크림 | 곁을 따라가고 있어요 |
| 잠들 준비 | 28% | 점점 느려짐 | 딥 앰버 | 오늘의 빛을 접고 있어요 |
| 잠듦 | 8% | 거의 없음 | 엠버 잔광 | 불씨만 남겨두었어요 |
| 특별한 성장 | 85% | 한 번 부풀었다 안정 | 앰버+라일락 | 새로운 온기를 기억했어요 |
| 오류·연결 끊김 | 12% | 없음 | 차가운 블루 | 잠시 목소리가 닿지 않아요 |

오류 상태에 강한 빨간 화면을 사용하지 않는다. 사용자의 조치가 꼭 필요한 작은 오류 표시에만 절제된 적색을 사용한다.

---

## 15. 모션 디자인

모션은 존재감을 만들기 위한 것이며, 화면을 화려하게 보이게 하기 위한 것이 아니다.

### 15.1 도깨비불 호흡

- 주기: `3.8–5.2초`
- 크기 변화: `scale(0.98)` ↔ `scale(1.025)`
- 밝기 변화: 최대 10% 이내
- 반복은 자연스럽고 약간 불규칙해 보여야 한다.

```css
@keyframes flame-breathe {
  0%, 100% { transform: scale(0.985) rotate(-0.4deg); opacity: 0.88; }
  48% { transform: scale(1.025) rotate(0.5deg); opacity: 1; }
}
```

### 15.2 카드 등장

- 아래에서 `8–12px` 이동하며 페이드 인
- 지속 시간 `420–620ms`
- 카드별 지연은 `40–70ms`
- 6개 이상의 요소에 연속 지연을 적용하지 않는다.

### 15.3 터치 반응

- 누를 때 `scale(0.985)`
- 지속 시간 `120–160ms`
- hover에서 카드를 위로 크게 띄우지 않는다.
- 글로우는 hover 시 5–10%만 강해진다.

### 15.4 모션 감소 설정

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    scroll-behavior: auto !important;
    transition-duration: 0.01ms !important;
  }
}
```

### 금지 모션

- 빠르게 번쩍이는 네온
- 지속적으로 떠다니는 카드
- 화면 전체를 도는 오로라
- 이유 없는 파티클 폭발
- 클릭할 때마다 큰 빛의 파동
- 로딩이 끝나도 계속 회전하는 장식

---

## 16. 반응형 및 접근성

### 16.1 모바일 우선

- 320–359px: 카드 내부 패딩 16px, 좌우 여백 16px
- 360–480px: 기본 규격 사용
- 481px 이상: 앱 셸 최대 520px, 좌우 중앙 정렬
- 768px 이상에서도 콘텐츠를 억지로 가로로 늘리지 않는다.

### 16.2 접근성

- 본문 텍스트 대비는 WCAG AA 수준을 목표로 한다.
- 작은 회색 글자에 `--text-disabled`를 사용하지 않는다.
- 상태를 색만으로 전달하지 않고 텍스트 또는 아이콘을 함께 표시한다.
- 모든 버튼과 탭의 터치 영역은 최소 `44 × 44px`.
- 키보드 포커스는 `2px solid rgba(249, 220, 160, 0.72)`로 분명하게 표시한다.
- 배경 블러를 지원하지 않는 브라우저에서도 카드 텍스트가 읽혀야 한다.
- 주요 정보를 배경 이미지 위에 직접 올리지 않는다.

```css
@supports not (backdrop-filter: blur(1px)) {
  .glass-card {
    background: rgba(20, 23, 32, 0.96);
  }
}
```

---

## 17. 완성도 검수 체크리스트

구현이 끝나면 아래 항목을 하나씩 확인한다.

### 전체 인상

- [ ] 첫눈에 일반 스마트홈 앱보다 ‘작은 존재와 함께하는 밤’이 느껴지는가?
- [ ] 화면의 75% 이상이 충분히 어둡고 조용한가?
- [ ] 가장 먼저 보이는 요소가 현재 도깨비불인가?
- [ ] 주인공 글로우가 한 개로 제한되어 있는가?
- [ ] 블루·퍼플·앰버가 서로 경쟁하지 않는가?

### 카드

- [ ] 카드가 단순 회색 박스가 아니라 미세한 깊이와 표면감을 가지는가?
- [ ] 모든 카드가 똑같이 밝거나 화려하지 않은가?
- [ ] 카드 테두리가 1px의 낮은 대비로 통일되어 있는가?
- [ ] 컬러 글로우가 카드 안쪽의 일부 영역에만 존재하는가?
- [ ] 카드 모서리와 내부 여백이 토큰에 맞는가?

### 텍스트

- [ ] 텍스트에 네온 그림자가 없는가?
- [ ] 한 화면에 가장 큰 텍스트가 한 개뿐인가?
- [ ] 숫자보다 감정적 상태 문장이 먼저 읽히는가?
- [ ] 작은 보조 텍스트도 실제 기기에서 읽을 수 있는가?
- [ ] 문구가 기계적 명령보다 세계관 언어를 사용하는가?

### 상호작용

- [ ] 버튼과 슬라이더의 터치 영역이 44px 이상인가?
- [ ] 상태 변화가 색뿐 아니라 문구로도 전달되는가?
- [ ] 모션이 느리고 미세하며 사용을 방해하지 않는가?
- [ ] 모션 감소 설정을 지원하는가?
- [ ] 로딩·빈 상태·오류 상태도 같은 디자인 언어를 유지하는가?

### 피해야 할 결과

- [ ] RGB 게이밍 UI처럼 보이지 않는가?
- [ ] 금융·AI SaaS 대시보드처럼 보이지 않는가?
- [ ] 지나치게 귀여운 육성 게임처럼 보이지 않는가?
- [ ] 의미 없는 그라데이션 장식이 없는가?
- [ ] 불필요한 배지, 아이콘, 차트가 없는가?

---

## 18. 개발 중 의사결정 기준

새로운 컴포넌트나 기능을 추가할 때 아래 질문을 순서대로 확인한다.

1. 이 정보는 도깨비불의 **현재 상태**, **함께한 기억**, **성장**, **시스템 설정** 중 어디에 속하는가?
2. 이 요소가 따뜻한 빛을 받을 이유가 있는가?
3. 이 요소가 현재 화면의 주인공보다 더 밝아지지는 않는가?
4. 숫자보다 먼저 보여줄 수 있는 상태 문장이 있는가?
5. 별도 카드가 정말 필요한가, 기존 카드의 보조 정보로 넣을 수 있는가?
6. 빛이나 모션을 제거해도 기능이 이해되는가?

판단이 어려울 경우 다음 원칙을 선택한다.

- 더 적은 색
- 더 약한 빛
- 더 넓은 여백
- 더 짧은 문장
- 더 느린 움직임
- 더 적은 카드

---

## 19. AI 코딩 도구에 전달할 실행 지시문

아래 문장을 구현 요청의 맨 앞에 붙여 사용할 수 있다.

```text
프로젝트 루트의 디자인.md를 디자인의 단일 기준 문서로 사용한다.
구현 전에 문서 전체를 읽고, 먼저 디자인 토큰을 전역 CSS 변수로 등록한다.
임의의 색상·그림자·border-radius 값을 추가하지 말고 문서의 토큰을 재사용한다.

화면은 Aurora Glassmorphism + Glow Bento UI 스타일로 구현하되,
일반적인 화려한 AI SaaS 또는 스마트홈 대시보드처럼 만들지 않는다.
배경은 깊이 있는 차콜 블랙과 딥 네이비로 유지하고,
차가운 블루·인디고는 공간과 기록에만,
크림·앰버는 살아 있는 도깨비불과 핵심 교감에만 사용한다.

한 화면에서 가장 강한 글로우는 현재 도깨비불 카드 하나로 제한한다.
나머지 카드는 낮은 대비의 검은 유리 표면으로 표현한다.
텍스트에는 네온 효과를 사용하지 않는다.
기능 수치보다 도깨비불의 상태를 설명하는 문장을 먼저 보여준다.

구현 완료 후 디자인.md의 17번 체크리스트를 기준으로 자체 검수하고,
위반한 항목이 있으면 수정한 뒤 결과를 제시한다.
```

---

## 20. 최종 디자인 정의

이 앱의 디자인은 ‘어두운 배경에 예쁜 그라데이션을 얹은 UI’가 아니다.

**밤이라는 넓고 조용한 공간 속에서, 사용자가 함께한 시간과 도깨비불의 상태가 몇 개의 작은 빛으로만 드러나는 인터페이스**다. 차가운 색은 밤의 깊이를 만들고, 따뜻한 색은 살아 있는 존재를 증명한다. 모든 시각 요소는 이 대비를 지키는 방향으로 결정한다.

