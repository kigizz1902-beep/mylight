# 반려 도깨비불 — 개발 프롬프트 (feat/dokkaebi-app-glassmorphism 기준, 재작성)

기준 브랜치: `feat/dokkaebi-app-glassmorphism` (main에서 분기, 2개 커밋)
기준 문서: `반려_도깨비불_A버전_최종발표_스토리보드_v6.md`, `디자인.md`, `디자인_ver2.md`

> 이 문서는 main 기준으로 작성했던 이전 버전(1~8단계)을 **폐기하고 다시 쓴 것**이다. 이 브랜치가 사용자가 지정한 "최종" 구현이며, main에서 진행하던 관계 상태 스토어/호흡광 엔진/이름 짓기 페이지는 이 브랜치의 `use-dokkaebi-app.ts` + `dokkaebi.ts` + `FirstMeet.tsx`가 이미 더 완성도 높게 대체하고 있어 폐기했다.

---

## 이미 구현되어 있는 것 (재작업 금지 — 확장만 할 것)

| 스토리보드 장면 | 브랜치 구현 위치 | 상태 |
|---|---|---|
| SCENE 1~2 (발견하기, 이름 짓기) | `src/components/dokkaebi/screens/FirstMeet.tsx`, `useDokkaebiApp.onTap/wake/saveName` | 완료. 5회 탭 각성 + 균열 애니메이션 + 이름 저장 |
| SCENE 3 (비 오는 밤, 상태별 호흡 골격) | `src/lib/dokkaebi.ts moodFor()`, `Flame.tsx`의 `breathe`/`slowing`/`none` 모션 | 부분 완료 — `awake`(breathe)/`winding`(slowing)/`asleep`(none) 3가지는 있으나 "지친 밤" 전용 상태 없음 (아래 갭 참고) |
| SCENE 6 (밝기 직접 조절) | `useDokkaebiApp.onBrightness`, `NightHome.tsx` 슬라이더 | 완료 (단, 저장만 하고 선호 누적/분류는 안 함) |
| SCENE 7 전반부 (최저 밝기 취침 신호, FR-04) | `useDokkaebiApp.onBrightness` — `v <= 3`일 때 `bedtimeAck`+`acking` 펄스 | 완료 |
| SCENE 8 (즉시 소등 + 기록) | `useDokkaebiApp.togglePower`, `TonightRecord.tsx` | 완료 — 지연/잔광 없이 즉시 전환, 세션 기록 저장 |
| SCENE 9 (부재 방어, 몽타주 개념) | `useDokkaebiApp.advance/nextNight`, `SettingsScreen.tsx`의 "사용하지 않는 기간에도 기록은 줄지 않습니다" | 부분 완료 — 밤 자동 누적은 되나 "아직 달라진 건 없어요" 축적 컷 문구는 없음 |
| SCENE 10 (7번째 밤 성장 단서) | `STAGES`(`at:7, key:"memory"`)가 있는 `src/lib/dokkaebi.ts`, `celebrating` 스웰 연출 | 부분 완료 — 성장 단계 전환은 있으나 "골드 앰버 2회 융기 후 자주 쓰던 밝기로 안정"이라는 구체 연출은 없음 |
| SCENE 11 (아침까지) | `TestTool`/`advance` 로 밤 이동은 되나 아침 시간 흐름 연출 자체는 없음 | 미구현 |
| SCENE 12 (성장 단계로서의 30번째 밤) | `STAGES`(`at:30, key:"character"`) | 부분 완료 — "성격 형성" 단계 텍스트만 있고, 날씨·밝기 기반 개체성/2단 공개/불꽃 형태 변화는 없음 |
| 기록/설정/네비게이션 | `HistoryLog.tsx`, `SettingsScreen.tsx`, `BottomNav.tsx` | 완료 |
| 개발용 시간 이동 도구 | `TestTool.tsx` (`nextNight`, `jumpSeven`, `jumpThirty`, `toggleRain`, `reset`) | 완료 |

---

## 발견한 핵심 갭 (중요도 순)

1. **감정 대화 자체가 없다.** SCENE 4(`오늘은 어떤 마음으로 돌아왔어?`)와 SCENE 5(`빛의 대답` — 스토리보드가 "제품의 핵심 발명"이라 부르는 장면)가 코드 어디에도 없다. `grep -rn "지쳤\|질문\|기분 좋은..." src/`가 완전히 빈 결과. 감정 선택지, `firstEmotionAnswer` 저장, 응답 프리셋이 전부 없다.
2. **SCENE 7 후속 질문이 없다.** 취침 시 밝기 최저 반응(FR-04)은 있지만, "아까 지쳤다고 했지. 지금은 어때?"라는 **기억 기반 후속 질문**은 없다. 이게 없으면 스토리보드가 강조하는 "관계의 결정적 증거"가 빠진다.
3. **밝기 선호가 값으로만 있고 선호로 분류/누적되지 않는다.** `state.brightness`는 그 순간의 슬라이더 값일 뿐, "이 사용자는 대부분 낮은 밝기에 머문다"는 걸 계산할 상태(예: 3단계 분류, 밤별 기록)가 없다.
4. **날씨 히스토리가 없다.** `state.rainy`는 현재 순간의 불리언 토글일 뿐, 30일 중 며칠 비가 왔는지 누적하는 배열이 없다. SCENE 12의 `함께한 30번의 밤 중 14번, 비가 내렸어요` 문장을 만들 데이터가 없다.
5. **30번째 밤 2단 공개(방 먼저, 앱 나중) 순서가 없다.** 현재 `DokkaebiApp.tsx`는 왼쪽 앱과 오른쪽 방이 항상 동시에 반응한다. 스토리보드 6장 조건 7번("30번째 밤의 변화는 앱보다 생활 공간의 빛에서 먼저 보여야 한다")을 어긴다.
6. **불꽃 형태가 안 바뀐다.** `Flame.tsx`는 밝기(intensity)와 움직임(motion)만 바꾸고, 30번째 밤 이후의 "물결치는 실루엣" 같은 형태 변화 로직이 없다.
7. **축적 컷 문장이 없다.** SCENE 9의 `아직 달라진 건 없어요. 다만 쌓이고 있어요.`가 화면에 없다.

이 7개가 스토리보드 5장 MUST 목록의 상당 부분과 겹친다. 아래 단계는 이 갭들을 **기존 파일을 확장**해서 메우는 순서다.

---

## 진행 규칙 (공통)

- 새 파일을 함부로 만들지 말 것. 위 표에 나온 기존 파일(`use-dokkaebi-app.ts`, `dokkaebi.ts`, `NightHome.tsx`, `DokkaebiApp.tsx`, `Flame.tsx`, `RoomScene.tsx`)을 확장하는 것을 기본으로 한다.
- 이 브랜치는 `디자인.md`의 톤(§ 번호, MUST 01~10, 절제된 글래스모피즘, 색상 규칙 — 방=차가움, 도깨비불=따뜻함, 성장=라일락)을 따르고 있으므로, 새로 쓰는 문구나 색도 그 규칙을 따른다. 스토리보드 v6와 디자인.md가 다른 표현을 쓰는 경우(`실제 조명` vs `생활 공간의 빛` 등) 스토리보드 v6 쪽 표현을 화면 문구로 채택한다.
- 각 단계 시작 전 관련 파일을 다시 읽고, 끝나면 `npm run build` (또는 최소 `npx tsc --noEmit -p .`)와 `npx vitest run`을 실행해 에러 없는지 확인한다.
- 각 단계는 `TestTool.tsx`의 개발용 버튼으로 수동 재현 가능해야 한다 (필요하면 새 테스트 버튼 추가).
- 완료 후 다음 단계로 자동으로 넘어가지 말고, 빌드/테스트 결과와 수동 확인 내용을 짧게 보고한다.

---

## 1단계 — 감정 상태 확장 (데이터 레이어)

파일: `src/lib/dokkaebi.ts`, `src/hooks/use-dokkaebi-app.ts`

- `DokkaebiState`에 추가: `firstEmotionAnswer: string | null`, `emotionAskedTonight: boolean`, `followUpAnswer: string | null`
- `moodFor()`에 `resting` mood 분기 추가 (SCENE 5): `firstEmotionAnswer`가 있고 `bedtimeAck`가 아직 아닐 때 → `headline: "오늘은 천천히 곁에 있을게"`, `motion: "slowing"`, 기존 `winding`보다는 덜 낮은 강도로 구분되는 `tone`
- 액션 추가: `answerEmotion(choice: string)` — 처음 답변만 저장, `saveName` 이후 `home` 화면 진입 시점부터 사용 가능
- `hasJong`/`gwa`/`iga` 등 기존 헬퍼는 그대로 재사용

**완료 조건**: 새 상태 전이를 커버하는 단위 테스트(`vitest`, 훅을 직접 렌더링하는 방식으로) 통과.

---

## 2단계 — SCENE 4/5: 감정 질문 카드 + 빛의 대답

파일: `src/components/dokkaebi/screens/NightHome.tsx` (Living Card 내부 확장)

- Living Card 안, `Flame` 아래에 작은 대화 카드 추가 — **전면 팝업 금지** (스토리보드 6장 조건 1번)
- 4개 선택지 버튼: `기분 좋은 일이 있었어` · `조금 지쳤어` · `마음이 복잡해` · `그냥 조용히 있고 싶어`
- 이미 답했거나 오늘 밤 화면을 벗어났다 돌아오면 카드는 사라지고 평소 홈 화면만 보임 (건너뛰기 허용 — 조건 6번)
- `조금 지쳤어` 선택 시: `useDokkaebiApp.answerEmotion` 호출 → mood가 `resting`으로 전환되며 밝기가 살짝 낮아지고, 화면에 `알겠어. 오늘은 천천히 곁에 있을게.` 표시
- 재사용: `glassPanel`, `glassButton`, `text`, `space` 등 기존 `design.ts`/`glass.ts` 토큰 그대로 사용

**완료 조건**: 답하지 않고 넘어가도 조명 사용(밝기 조절, 소등)이 막히지 않는지, 답하는 경로와 건너뛰는 경로 둘 다 수동 확인.

---

## 3단계 — SCENE 7: 취침 후속 질문 (기억)

파일: `src/hooks/use-dokkaebi-app.ts`(`onBrightness`), `src/components/dokkaebi/screens/NightHome.tsx`

- `onBrightness`에서 `bedtimeAck`가 이번에 처음 true가 되는 순간(FR-04 펄스) **이후**, `firstEmotionAnswer`가 있으면 약간의 시간차를 두고 후속 질문 카드를 띄운다 (스토리보드 주의사항: 두 반응이 하나로 뭉쳐 보이지 않게 간격을 둘 것)
- 질문: `아까 지쳤다고 했지. 지금은 어때?` / 선택지: `조금 편해졌어` · `아직 그대로야` · `그냥 말없이 쉬고 싶어`
- 선택지 중 어느 것도 실패/성공으로 평가하지 않는다 — 셋 다 낮은 수면 빛 유지, `조금 편해졌어`만 크림빛 1회 반짝임 후 복귀
- 응답 후 문장: `그럼 오늘은 이 빛으로 함께 있을게.`
- 답하지 않아도 취침/소등/기록은 정상 진행 (조건 6번)

**완료 조건**: 최저 밝기 도달 → (짧은 간격) → 후속 질문 → 선택 → 소등까지 한 번의 시연 경로로 끊김 없이 확인.

---

## 4단계 — 밝기 선호 누적 + 날씨 히스토리 (SCENE 12 데이터 기반)

파일: `src/lib/dokkaebi.ts`, `src/hooks/use-dokkaebi-app.ts`

- `NightSession`에 `rainy: boolean`, `lowBrightness: boolean`(그 밤 낮은 밝기에 오래 머물렀는지) 필드 추가
- `advance()`(현재 `nextNight`/`jumpSeven`/`jumpThirty`가 호출하는 함수)가 밤을 생성할 때 결정적 패턴으로 `rainy`를 채워 넣는다 — 스토리보드 SCENE 9/12-A와 맞춰 비가 여러 번 반복되도록 (예: `n % 3 === 0`가 아니라 스토리보드가 명시한 "14/30번" 비율에 가깝게 결정적 시퀀스 하나 정의)
- 순수 함수 추가: `getWeatherSummarySentence(sessions)` → `함께한 30번의 밤 중 N번, 비가 내렸어요.`, `getBrightnessPreferenceSentence(sessions)` → 낮은 밝기 세션이 과반이면 `그리고 당신은 대부분의 밤, 낮은 빛에 오래 머물렀어요.` (mid/high는 SHOULD 범위라 지금은 생략 가능)
- 두 문장은 **독립적으로** 계산해서 인과관계가 섞이지 않게 한다 (v6 변경사항 #4)

**완료 조건**: `jumpThirty` 실행 후 두 문장이 실제 누적된 세션 데이터에서 계산되어 나오는지 단위 테스트로 검증.

---

## 5단계 — SCENE 9 축적 컷 + SCENE 10 디테일 보강

파일: `src/components/dokkaebi/DokkaebiApp.tsx`, `src/components/dokkaebi/screens/NightHome.tsx` 또는 `HistoryLog.tsx`

- `advance()`로 밤이 여러 개 건너뛰어질 때(2~6번째 밤 자동 진행), 마지막에 3초 정도 `아직 달라진 건 없어요. 다만 쌓이고 있어요.` 문구만 보여주는 짧은 화면/오버레이 삽입 — 빛은 평소 호흡 그대로 유지
- 7번째 밤 도달 시(`stage.at === 7`) `celebrating` 스웰을 "골드 앰버 2회 융기"로 구체화 (현재는 1회성 swell 모션만 있음 — `Flame.tsx`의 `flame-field--swell` 애니메이션을 2연속으로 재생하도록 `globals.css` 키프레임 조정)
- 안정 후 밝기가 `getBrightnessPreferenceSentence`가 계산한 선호값 근처로 슬라이더 값을 자동 이동

**완료 조건**: `TestTool`의 `jumpSeven` 실행 시 골드 융기 2회 → 낮은 밝기 안정 → `함께한 일곱 번째 밤. 당신 곁이 조금 익숙해졌어요.` 문구까지 수동 확인.

---

## 6단계 — SCENE 12: 2단 공개 + 불꽃 형태 변화

파일: `src/components/dokkaebi/DokkaebiApp.tsx`, `src/components/dokkaebi/RoomScene.tsx`, `src/components/dokkaebi/Flame.tsx`, `src/app/globals.css`

- `state.nights === 30` 도달 순간: 먼저 `RoomScene`의 빛(색·밝기·리듬)만 물결 기질로 바뀌고, 앱 쪽 화면(왼쪽 `DokkaebiApp` 패널)은 수 초간 어둡게/비활성 상태 유지 → 이후 앱이 밝아지며 4단계에서 만든 두 문장 표시 (스토리보드 6장 조건 7번)
- `Flame.tsx`에 `temperament?: "none" | "water"` prop 추가, `water`일 때 `globals.css`의 `.flame` 실루엣에 세로로 길고 끝이 물결치는 형태 변형 keyframe/`border-radius` 애니메이션 추가 — 물리적 형태 자체를 바꾸는 게 아니라 CSS로 표현되는 정도로 (스토리보드 제외 목록: "물리 조명 오브제의 형태가 변하는 것처럼 보이는 연출"은 제외이므로, 이 변화는 **앱 속 불꽃**에만 적용하고 `RoomScene`의 물리 오브제 실루엣은 그대로 둔다 — 색·밝기·리듬만 바꾼다)
- `모루는 비를 오래 머금은 불이 되었습니다.` 문구와 함께 위 상태 전환

**완료 조건**: `jumpThirty` 실행 시 오른쪽 방의 빛이 먼저 바뀌고, 뒤이어 왼쪽 앱이 두 독립 문장과 함께 밝아지는 순서가 수동으로 확인됨. 물리 오브제(공예 형태)는 형태가 바뀌지 않고 색/리듬만 바뀌는지 확인.

---

## 7단계 — 발표 리허설 통합

- 스토리보드 4장의 7개 조작 포인트가 이 브랜치의 화면 흐름과 정확히 맞는지 점검:
  1. 발견하기 → 이름 `모루` (기존, 완료)
  2. 비 오는 밤 켜기 → 호흡 확인 (기존, 완료)
  3. `조금 지쳤어` 선택 (2단계)
  4. 밝기 낮춤 → 최저 밝기 취침 (기존 + 4단계 데이터 누적)
  5. `조금 편해졌어` → 소등 (3단계)
  6. 몽타주 후 7번째 밤 (5단계)
  7. 시간 압축 후 30번째 밤 (4·6단계)
- `TestTool.tsx`의 버튼들이 위 흐름을 그대로 재현할 수 있는지 확인하고, 발표 리허설에 불필요한 개발용 버튼은 `process.env.NODE_ENV !== "production"` 가드로 이미 숨겨지는지 재확인
- `/` 진입부터 30번째 밤까지 새로고침 없이 한 번에 재생, 3분 내외 타이밍 점검

**완료 조건**: 전체 경로 무중단 재생 + `npm run build` 프로덕션 빌드 통과.

---

## 참고 — 이번 라운드에서 계속 제외

- 실물 조명(Hue/Yeelight) 연동, 음성 인식, 음악 싱크, 감정 점수, 부재 페널티, 색상 직접 선택 팔레트, 물리 오브제 형태 변화 — 스토리보드 5장 "제외" 목록 그대로 유지.
